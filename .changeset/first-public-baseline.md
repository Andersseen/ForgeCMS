---
---

Establish the first public `0.0.1` release baseline.

Historical pre-public changesets were cleared so the first release does not calculate later internal
versions. Public package manifests are already aligned to `0.0.1`; future package changes should add
normal changesets again.
