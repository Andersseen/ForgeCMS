---
'@forge-cms/db': minor
'@forge-cms/cloudflare': minor
'@forge-cms/testing': minor
---

Add `count(collection)` to the `DatabaseAdapter` contract so callers can get record counts without fetching every row.
