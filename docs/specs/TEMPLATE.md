# NNN — Title (imperative: "Add X", "Migrate Y")

- **Status:** draft <!-- draft | approved | in-progress | done | rejected | superseded -->
- **Author:** <!-- human name or "agent draft" -->
- **Date:** <!-- YYYY-MM-DD -->
- **Branch:** <!-- fill when in-progress -->
- **Affected packages/apps:** <!-- e.g. @forge-cms/runtime, apps/www -->

## Context / Why

<!-- 2-5 sentences. What problem exists, why now. Link to STATE.md known-issue numbers or other specs if relevant. -->

## Goal

<!-- One sentence. The single observable outcome when this is done. -->

## Non-goals

<!-- Bullet list. Things a reasonable implementer might assume are included but are NOT. -->

## Design

<!-- The heart of the spec. Show the exact public API surface: exported types, function signatures,
     endpoint shapes, component selectors. Note behavior edge cases (invalid input, empty states).
     If changing a stable contract (adapter interface, API envelope), show before/after. -->

## Implementation plan

<!-- Ordered checklist of small steps. The implementer ticks these off as they go.
- [ ] step 1 (file/package it touches)
- [ ] step 2
- [ ] tests
- [ ] changeset + STATE.md update
-->

## Test plan

<!-- Exactly which tests prove it works: unit tests to add, contract suites to run,
     manual checks (e.g. "pnpm dev:www → /admin/collections shows X"). -->

## Acceptance criteria

<!-- Numbered, each one mechanically checkable. The spec is "done" when all pass.
1. ...
2. `pnpm lint && pnpm typecheck && pnpm test && pnpm build` green.
-->

## Open questions

<!-- Must be empty before status can become "approved". Move answers into Design. -->

## Outcome

<!-- Fill when done: one line on what shipped, plus any divergence from the plan. -->
